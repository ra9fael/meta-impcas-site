# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend \
            "

BBFILE_COLLECTIONS += "impcas-util"
BBFILE_PATTERN_impcas-util = "^${LAYERDIR}/"
BBFILE_PRIORITY_impcas-util = "5"

# Hardware-agnostic services and tools shared across IMPCAS projects:
# site-configuration from the BOOT partition, FPGA bitstream loading, and
# similar plumbing that does not depend on a machine or on EPICS.
# Board-specific recipes (device tree, kernel, u-boot) belong to the project's
# meta-user, EPICS recipes to meta-epics.

LAYERSERIES_COMPAT_impcas-util = "scarthgap"

LAYERDEPENDS_impcas-util = "core"
